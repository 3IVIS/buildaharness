{ "id": "19-ask-user-options-and-question", "description": "ASK_USER round-trips its question + options identically (S3 — a wired action; fromJSON/toJSON is unchanged by the wiring)", "directive_in": { "action": "ASK_USER", "rationale": "two plausible deploy targets, no way to pick", "question": { "question": "which environment should this deploy to?", "options": ["staging", "production", "  ", "canary"] } } }
