apiVersion: v1
kind: Service
metadata:
  name: {{ include "buildaharness.fullname" . }}-adapter
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "buildaharness.labels" . | nindent 4 }}
    app.kubernetes.io/component: adapter
spec:
  type: {{ .Values.adapter.service.type }}
  ports:
    - port: {{ .Values.adapter.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "buildaharness.selectorLabels" (dict "root" . "component" "adapter") | nindent 4 }}
---
{{- if .Values.adapter.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "buildaharness.fullname" . }}-adapter
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "buildaharness.labels" . | nindent 4 }}
    app.kubernetes.io/component: adapter
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "buildaharness.fullname" . }}-adapter
  minReplicas: {{ .Values.adapter.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.adapter.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.adapter.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.adapter.autoscaling.targetMemoryUtilizationPercentage }}
{{- end }}
---
{{- if .Values.adapter.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "buildaharness.fullname" . }}-adapter
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "buildaharness.labels" . | nindent 4 }}
    app.kubernetes.io/component: adapter
spec:
  minAvailable: {{ .Values.adapter.pdb.minAvailable }}
  selector:
    matchLabels:
      {{- include "buildaharness.selectorLabels" (dict "root" . "component" "adapter") | nindent 6 }}
{{- end }}
