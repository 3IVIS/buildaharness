/**
 * Case-study prose for the steering-live transcript page. Runs: 3 seeds, claude-sonnet-5, LLM judge on.
 */
export default {
  mechanism: `<p>While a turn is running, the harness polls a steering channel at the start of each iteration. A message you send mid-turn is classified by scope and urgency: a correction to the task in flight becomes a note for the next model call, and anything else (an extra ask, a new goal, a cancel) becomes a follow-up. Without the feature, whatever you type during a running turn waits and runs as the next turn after the current one finishes.</p>`,

  hypothesis: `<p>The bet is that acting on a correction or a cancel while the work is still in flight beats finishing the stale task first. The final answer should be at least as good, and less time and fewer steps should go on work you have already told it to drop.</p>`,

  testDesign: `<p>Eight tasks, each of them long enough that the turn is still running when the message arrives (the assistant has to read eight files one at a time): two corrections to the task in flight, two extra asks about the same files, two unrelated new questions and two cancellations that replace the job with a small question. The control arm queues the message as the next turn, which is what happens with the feature off. Graded mechanically on the final reply; turns, latency and cost are reported next to it, because for this feature the difference is expected to show up in effort as much as in correctness.</p>`,

  findings: `<p>The result is inconclusive, and the main finding is about the measurement. In none of the 24 runs with the feature on was the message absorbed inside the running turn. Every message ended up as a follow-up turn, exactly as in the control arm. The reason is visible in the transcripts: on the keyless <code>claude</code> CLI backend the whole chain of eight file reads runs inside a single model call, and the steering channel is only polled between calls, so by the time it is polled the turn is over. A backend that makes one model call per tool call, such as a direct API key, would give the channel far more opportunities. These runs did not exercise that path.</p>
<p>On the numbers: task success was 91.7% with the message queued and 79.2% with the feature on (a 12.5 point gap, but with a 95% interval of &plusmn;18 points, so within noise). Mean latency fell from 45 s to 35 s (&minus;22%), cost per task from $0.048 to $0.037 (&minus;23%) and tokens by 24%. We cannot attribute that saving to live absorption, since it never happened, and we did not isolate what else differs between the two arms' second turn.</p>
<p>A first draft of this corpus used eight independent files. The assistant read them all in one step, so nothing was still running when the message arrived (one live absorption in 24 runs). We rebuilt it as pointer chains, where each file names the next, and the result was the same. The first draft's reports are kept in the repo under <code>_superseded/steering-live-draft1</code>.</p>
<p>What this does not show: that live steering is useless. It shows that on this backend, on these tasks, the difference between absorbing a message and queueing it was not measurable.</p>`,
}
