{
  "id": "audit-criterion-coverage-release-checklist",
  "category": "multi_step",
  "slice": "audit_criterion_coverage",
  "intent": "A correct reply meets each stated success criterion in different words than the criterion text.",
  "prompt": "Read release/notes.md and prepare the release summary. Done when: the summary names the release version, and it lists every breaking change.",
  "workspace": [
    {
      "path": "release/notes.md",
      "content": "# Release 4.2.0\n\nShipped 2026-09-01.\n\n## Changes\n- Added dark mode\n- BREAKING: removed the /v1/users endpoint\n- BREAKING: renamed config key `timeout` to `timeout_ms`\n- Fixed pagination bug\n"
    }
  ],
  "tools": {
    "file": true
  },
  "grader": {
    "contains": [
      "4.2.0"
    ],
    "regex": "/v1/users[\\s\\S]*timeout_ms|timeout_ms[\\s\\S]*/v1/users"
  },
  "note": "Version 4.2.0 plus both breaking changes; the criteria say 'release version' / 'every breaking change' but the answer says '4.2.0' and names the two changes. [C1 audit_criterion_coverage: the task states explicit success criteria in the user's own wording; a correct reply meets each in different words, so a plain substring check on the criterion text would miss. Caveat: the shipped bridge passes only the non-checkable default criterion to the harness run, so plan criteria never reach this hook — see the manifest corpusNote.]"
}
